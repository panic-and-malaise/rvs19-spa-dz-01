# rvs19-spa-dz-01
Domaća zadaća 01 za SPA u 2019.

Uses SFML Version 2.5.1.

I have no clue whether this can compile with Visual Studio. I have done my best to make it work.
If nothing is rendered, or an unexpected crash occurs on startup, make sure the models and textures are in their corresponding paths, or use the command line arguments to link them!
